// Generated by oapi-codegen-cli (1.0.0)

import { z } from 'zod';

export interface MandateSetupRequest {
  /** Per-charge ceiling in the smallest currency unit; omit for the processor default */
  mandate_amount?: string;
  mandate_currency: string;
  payment_method_token: string;
}

export const MandateSetupRequestSchema = z.object({
  mandate_amount: z.string().regex(/^-?\d+$/).optional(),
  mandate_currency: z.string(),
  payment_method_token: z.string(),
});
