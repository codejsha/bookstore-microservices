// Generated by oapi-codegen-cli (1.0.0)

import { z } from 'zod';
import { RefundType, RefundTypeSchema } from '../constant/refund-type';

export interface RefundCreateRequest {
  /** Refund amount in smallest currency unit */
  amount: string;
  currency: string;
  /** Client-supplied key that makes retries of this request return the same refund */
  idempotency_key: string;
  metadata?: Record<string, unknown>;
  /** Payment ID to refund */
  payment_id: string;
  reason?: string;
  refund_type?: RefundType;
}

export const RefundCreateRequestSchema = z.object({
  amount: z.string().regex(/^-?\d+$/),
  currency: z.string(),
  idempotency_key: z.string(),
  metadata: z.record(z.string(), z.unknown()).optional(),
  payment_id: z.string(),
  reason: z.string().optional(),
  refund_type: RefundTypeSchema.optional(),
});
