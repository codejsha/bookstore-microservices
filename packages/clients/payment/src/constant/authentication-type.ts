// Generated by oapi-codegen-cli (1.0.0)

import { z } from 'zod';

export enum AuthenticationType {
  AuthenticationTypeThreeDs = 'three_ds',
  AuthenticationTypeNoThreeDs = 'no_three_ds',
}

export const AuthenticationTypeSchema = z.enum([
  'three_ds',
  'no_three_ds',
]);

/**
 * Parses an OpenAPI string value into a {@link AuthenticationType}.
 * @throws {Error} when the value is not a known AuthenticationType.
 */
export function parseAuthenticationType(value: string): AuthenticationType {
  if (isAuthenticationType(value)) {
    return value;
  }
  throw new Error(`Invalid AuthenticationType value: ${value}`);
}

/** Type guard returning whether `value` is a known {@link AuthenticationType}. */
export function isAuthenticationType(value: string): value is AuthenticationType {
  return (Object.values(AuthenticationType) as string[]).includes(value);
}
