// Generated by oapi-codegen-cli (1.0.0)

import { z } from 'zod';

export enum MandateType {
  MandateTypeSingleUse = 'single_use',
  MandateTypeMultiUse = 'multi_use',
}

export const MandateTypeSchema = z.enum([
  'single_use',
  'multi_use',
]);

/**
 * Parses an OpenAPI string value into a {@link MandateType}.
 * @throws {Error} when the value is not a known MandateType.
 */
export function parseMandateType(value: string): MandateType {
  if (isMandateType(value)) {
    return value;
  }
  throw new Error(`Invalid MandateType value: ${value}`);
}

/** Type guard returning whether `value` is a known {@link MandateType}. */
export function isMandateType(value: string): value is MandateType {
  return (Object.values(MandateType) as string[]).includes(value);
}
