// Generated by oapi-codegen-cli (1.0.0)

import { z } from 'zod';

export enum PaymentMethodEnum {
  PaymentMethodEnumCard = 'card',
  PaymentMethodEnumWallet = 'wallet',
  PaymentMethodEnumBankTransfer = 'bank_transfer',
  PaymentMethodEnumPayLater = 'pay_later',
}

export const PaymentMethodEnumSchema = z.enum([
  'card',
  'wallet',
  'bank_transfer',
  'pay_later',
]);

/**
 * Parses an OpenAPI string value into a {@link PaymentMethodEnum}.
 * @throws {Error} when the value is not a known PaymentMethodEnum.
 */
export function parsePaymentMethodEnum(value: string): PaymentMethodEnum {
  if (isPaymentMethodEnum(value)) {
    return value;
  }
  throw new Error(`Invalid PaymentMethodEnum value: ${value}`);
}

/** Type guard returning whether `value` is a known {@link PaymentMethodEnum}. */
export function isPaymentMethodEnum(value: string): value is PaymentMethodEnum {
  return (Object.values(PaymentMethodEnum) as string[]).includes(value);
}
