// Generated by oapi-codegen-cli (1.0.0)

import { z } from 'zod';

export enum RefundStatus {
  RefundStatusPending = 'pending',
  RefundStatusSucceeded = 'succeeded',
  RefundStatusFailed = 'failed',
  RefundStatusReview = 'review',
}

export const RefundStatusSchema = z.enum([
  'pending',
  'succeeded',
  'failed',
  'review',
]);

/**
 * Parses an OpenAPI string value into a {@link RefundStatus}.
 * @throws {Error} when the value is not a known RefundStatus.
 */
export function parseRefundStatus(value: string): RefundStatus {
  if (isRefundStatus(value)) {
    return value;
  }
  throw new Error(`Invalid RefundStatus value: ${value}`);
}

/** Type guard returning whether `value` is a known {@link RefundStatus}. */
export function isRefundStatus(value: string): value is RefundStatus {
  return (Object.values(RefundStatus) as string[]).includes(value);
}
