// Generated by oapi-codegen-cli (1.0.0)

/**
 * Thrown by every PaymentApiClient method when the HTTP response is not 2xx.
 * Carries the status code and the response body parsed per the response's declared
 * content-type (JSON parsed object, text body for `text/*`, raw `Blob` for binary,
 * or `undefined` when the response had no body).
 */
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly statusText: string,
    public readonly body: unknown,
  ) {
    super(`HTTP ${status}: ${statusText}`);
    this.name = 'ApiError';
  }
}
import type { PaymentCreateRequest } from '../model/payment-create-request';
import type { PaymentFindAllResponse } from '../model/payment-find-all-response';
import type { PaymentFindResponse } from '../model/payment-find-response';
import type { PaymentUpdateRequest } from '../model/payment-update-request';
import type { PaymentUpdateResponse } from '../model/payment-update-response';
import { PaymentStatus } from '../constant/payment-status';

export interface Pageable {
  page?: number;
  size?: number;
  sort?: string;
}

/**
 * Credentials for PaymentApi. One field per declared security scheme;
 * supply the value for the scheme(s) the operation you call requires.
 *   - `api-key` schemes carry the raw key (set as the configured header / query / cookie).
 *   - `http-bearer` / `oauth2` / `openid-connect` carry the token (sent as `Authorization: Bearer …`).
 *   - `http-basic` carries the already-base64-encoded `username:password` (sent as `Authorization: Basic …`).
 */
export interface PaymentApiCredentials {
  /** Scheme `BearerAuth` (`http-bearer`). */
  bearerAuth?: string;
}

/**
 * Payment API
 */
export interface PaymentApi {

  /**
   * Find all payments
   */
  paymentsGetAll(
    customerId: string | undefined,
    status: PaymentStatus | undefined,
    connector: string | undefined,
    pageable: Pageable,
  ): Promise<PaymentFindAllResponse>;

  /**
   * Create payment
   *
   * Response headers (on success; read via `response.headers` — not part of the return value):
   * - `location`: string — Location of the created resource
   */
  paymentsCreate(
    body: PaymentCreateRequest,
  ): Promise<void>;

  /**
   * Find payment
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  paymentsRead(
    uid: string,
  ): Promise<PaymentFindResponse>;

  /**
   * Update payment
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  paymentsUpdate(
    uid: string,
    body: PaymentUpdateRequest,
  ): Promise<PaymentUpdateResponse>;
}

export class PaymentApiClient implements PaymentApi {
  private readonly baseUrl: string;
  private readonly credentials: PaymentApiCredentials;

  constructor(baseUrl: string, credentials: PaymentApiCredentials = {}) {
    this.baseUrl = baseUrl;
    this.credentials = credentials;
  }

  async paymentsGetAll(
    customerId: string | undefined,
    status: PaymentStatus | undefined,
    connector: string | undefined,
    pageable: Pageable,
  ): Promise<PaymentFindAllResponse> {
    const params = new URLSearchParams();
    if (customerId !== undefined) params.append('customer_id', Array.isArray(customerId) ? (customerId as readonly unknown[]).join(',') : String(customerId));
    if (status !== undefined) params.append('status', Array.isArray(status) ? (status as readonly unknown[]).join(',') : String(status));
    if (connector !== undefined) params.append('connector', Array.isArray(connector) ? (connector as readonly unknown[]).join(',') : String(connector));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/payments${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as PaymentFindAllResponse;
  }

  async paymentsCreate(
    body: PaymentCreateRequest,
  ): Promise<void> {
    const url = `${this.baseUrl}/api/v1/payments`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
  }

  async paymentsRead(
    uid: string,
  ): Promise<PaymentFindResponse> {
    const url = `${this.baseUrl}/api/v1/payments/${uid}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as PaymentFindResponse;
  }

  async paymentsUpdate(
    uid: string,
    body: PaymentUpdateRequest,
  ): Promise<PaymentUpdateResponse> {
    const url = `${this.baseUrl}/api/v1/payments/${uid}`;
    const response = await fetch(url, {
      method: 'PUT',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as PaymentUpdateResponse;
  }
}
