// Generated by oapi-codegen-cli (1.0.0)

/**
 * Thrown by every MandateApiClient method when the HTTP response is not 2xx.
 * Carries the status code and the response body parsed per the response's declared
 * content-type (JSON parsed object, text body for `text/*`, raw `Blob` for binary,
 * or `undefined` when the response had no body).
 */
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly statusText: string,
    public readonly body: unknown,
  ) {
    super(`HTTP ${status}: ${statusText}`);
    this.name = 'ApiError';
  }
}
import type { MandateFindAllResponse } from '../model/mandate-find-all-response';
import type { MandateFindResponse } from '../model/mandate-find-response';
import type { MandateSetupRequest } from '../model/mandate-setup-request';
import { MandateStatus } from '../constant/mandate-status';

export interface Pageable {
  page?: number;
  size?: number;
  sort?: string;
}

/**
 * Credentials for MandateApi. One field per declared security scheme;
 * supply the value for the scheme(s) the operation you call requires.
 *   - `api-key` schemes carry the raw key (set as the configured header / query / cookie).
 *   - `http-bearer` / `oauth2` / `openid-connect` carry the token (sent as `Authorization: Bearer …`).
 *   - `http-basic` carries the already-base64-encoded `username:password` (sent as `Authorization: Basic …`).
 */
export interface MandateApiCredentials {
  /** Scheme `BearerAuth` (`http-bearer`). */
  bearerAuth?: string;
}

/**
 * Mandate API
 */
export interface MandateApi {

  /**
   * Find all mandates
   */
  mandatesGetAll(
    customerId: string | undefined,
    mandateStatus: MandateStatus | undefined,
    pageable: Pageable,
  ): Promise<MandateFindAllResponse>;

  /**
   * Register a payment instrument and create the mandate that authorizes future charges
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   */
  mandatesSetup(
    body: MandateSetupRequest,
  ): Promise<MandateFindResponse>;

  /**
   * Find mandate
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  mandatesRead(
    uid: string,
  ): Promise<MandateFindResponse>;

  /**
   * Revoke mandate
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  mandatesRevoke(
    uid: string,
  ): Promise<MandateFindResponse>;
}

export class MandateApiClient implements MandateApi {
  private readonly baseUrl: string;
  private readonly credentials: MandateApiCredentials;

  constructor(baseUrl: string, credentials: MandateApiCredentials = {}) {
    this.baseUrl = baseUrl;
    this.credentials = credentials;
  }

  async mandatesGetAll(
    customerId: string | undefined,
    mandateStatus: MandateStatus | undefined,
    pageable: Pageable,
  ): Promise<MandateFindAllResponse> {
    const params = new URLSearchParams();
    if (customerId !== undefined) params.append('customer_id', Array.isArray(customerId) ? (customerId as readonly unknown[]).join(',') : String(customerId));
    if (mandateStatus !== undefined) params.append('mandate_status', Array.isArray(mandateStatus) ? (mandateStatus as readonly unknown[]).join(',') : String(mandateStatus));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/mandates${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as MandateFindAllResponse;
  }

  async mandatesSetup(
    body: MandateSetupRequest,
  ): Promise<MandateFindResponse> {
    const url = `${this.baseUrl}/api/v1/mandates`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as MandateFindResponse;
  }

  async mandatesRead(
    uid: string,
  ): Promise<MandateFindResponse> {
    const url = `${this.baseUrl}/api/v1/mandates/${uid}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as MandateFindResponse;
  }

  async mandatesRevoke(
    uid: string,
  ): Promise<MandateFindResponse> {
    const url = `${this.baseUrl}/api/v1/mandates/${uid}/revoke`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as MandateFindResponse;
  }
}
