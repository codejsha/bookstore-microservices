// Generated by oapi-codegen-cli (1.0.0)

/**
 * Thrown by every CustomerPaymentMethodApiClient method when the HTTP response is not 2xx.
 * Carries the status code and the response body parsed per the response's declared
 * content-type (JSON parsed object, text body for `text/*`, raw `Blob` for binary,
 * or `undefined` when the response had no body).
 */
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly statusText: string,
    public readonly body: unknown,
  ) {
    super(`HTTP ${status}: ${statusText}`);
    this.name = 'ApiError';
  }
}
import type { PaymentMethodCreateRequest } from '../model/payment-method-create-request';
import type { PaymentMethodFindAllResponse } from '../model/payment-method-find-all-response';
import type { PaymentMethodFindResponse } from '../model/payment-method-find-response';
import type { PaymentMethodUpdateRequest } from '../model/payment-method-update-request';
import type { PaymentMethodUpdateResponse } from '../model/payment-method-update-response';
import { PaymentMethodEnum } from '../constant/payment-method-enum';

export interface Pageable {
  page?: number;
  size?: number;
  sort?: string;
}

/**
 * Credentials for CustomerPaymentMethodApi. One field per declared security scheme;
 * supply the value for the scheme(s) the operation you call requires.
 *   - `api-key` schemes carry the raw key (set as the configured header / query / cookie).
 *   - `http-bearer` / `oauth2` / `openid-connect` carry the token (sent as `Authorization: Bearer …`).
 *   - `http-basic` carries the already-base64-encoded `username:password` (sent as `Authorization: Basic …`).
 */
export interface CustomerPaymentMethodApiCredentials {
  /** Scheme `BearerAuth` (`http-bearer`). */
  bearerAuth?: string;
}

/**
 * CustomerPaymentMethod API
 */
export interface CustomerPaymentMethodApi {

  /**
   * Find all payment methods of a customer
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  customerPaymentMethodsGetAll(
    customerUid: string,
    paymentMethod: PaymentMethodEnum | undefined,
    pageable: Pageable,
  ): Promise<PaymentMethodFindAllResponse>;

  /**
   * Create payment method for a customer
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   *
   * Response headers (on success; read via `response.headers` — not part of the return value):
   * - `location`: string — Location of the created resource
   */
  customerPaymentMethodsCreate(
    customerUid: string,
    body: PaymentMethodCreateRequest,
  ): Promise<void>;

  /**
   * Find payment method of a customer
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  customerPaymentMethodsRead(
    customerUid: string,
    uid: string,
  ): Promise<PaymentMethodFindResponse>;

  /**
   * Update payment method of a customer
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  customerPaymentMethodsUpdate(
    customerUid: string,
    uid: string,
    body: PaymentMethodUpdateRequest,
  ): Promise<PaymentMethodUpdateResponse>;

  /**
   * Delete payment method of a customer
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  customerPaymentMethodsDelete(
    customerUid: string,
    uid: string,
  ): Promise<void>;
}

export class CustomerPaymentMethodApiClient implements CustomerPaymentMethodApi {
  private readonly baseUrl: string;
  private readonly credentials: CustomerPaymentMethodApiCredentials;

  constructor(baseUrl: string, credentials: CustomerPaymentMethodApiCredentials = {}) {
    this.baseUrl = baseUrl;
    this.credentials = credentials;
  }

  async customerPaymentMethodsGetAll(
    customerUid: string,
    paymentMethod: PaymentMethodEnum | undefined,
    pageable: Pageable,
  ): Promise<PaymentMethodFindAllResponse> {
    const params = new URLSearchParams();
    if (paymentMethod !== undefined) params.append('payment_method', Array.isArray(paymentMethod) ? (paymentMethod as readonly unknown[]).join(',') : String(paymentMethod));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/customers/${customerUid}/payment-methods${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as PaymentMethodFindAllResponse;
  }

  async customerPaymentMethodsCreate(
    customerUid: string,
    body: PaymentMethodCreateRequest,
  ): Promise<void> {
    const url = `${this.baseUrl}/api/v1/customers/${customerUid}/payment-methods`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
  }

  async customerPaymentMethodsRead(
    customerUid: string,
    uid: string,
  ): Promise<PaymentMethodFindResponse> {
    const url = `${this.baseUrl}/api/v1/customers/${customerUid}/payment-methods/${uid}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as PaymentMethodFindResponse;
  }

  async customerPaymentMethodsUpdate(
    customerUid: string,
    uid: string,
    body: PaymentMethodUpdateRequest,
  ): Promise<PaymentMethodUpdateResponse> {
    const url = `${this.baseUrl}/api/v1/customers/${customerUid}/payment-methods/${uid}`;
    const response = await fetch(url, {
      method: 'PUT',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as PaymentMethodUpdateResponse;
  }

  async customerPaymentMethodsDelete(
    customerUid: string,
    uid: string,
  ): Promise<void> {
    const url = `${this.baseUrl}/api/v1/customers/${customerUid}/payment-methods/${uid}`;
    const response = await fetch(url, {
      method: 'DELETE',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
  }
}
