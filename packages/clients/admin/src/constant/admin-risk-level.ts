// Generated by oapi-codegen-cli (1.0.0)

import { z } from 'zod';

/**
 * Enforcement level for a flagged principal
 */

export enum AdminRiskLevel {
  AdminRiskLevelRestrict = 'restrict',
  AdminRiskLevelBlock = 'block',
}

export const AdminRiskLevelSchema = z.enum([
  'restrict',
  'block',
]);

/**
 * Parses an OpenAPI string value into a {@link AdminRiskLevel}.
 * @throws {Error} when the value is not a known AdminRiskLevel.
 */
export function parseAdminRiskLevel(value: string): AdminRiskLevel {
  if (isAdminRiskLevel(value)) {
    return value;
  }
  throw new Error(`Invalid AdminRiskLevel value: ${value}`);
}

/** Type guard returning whether `value` is a known {@link AdminRiskLevel}. */
export function isAdminRiskLevel(value: string): value is AdminRiskLevel {
  return (Object.values(AdminRiskLevel) as string[]).includes(value);
}
