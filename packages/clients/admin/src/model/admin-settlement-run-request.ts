// Generated by oapi-codegen-cli (1.0.0)

import { z } from 'zod';

/**
 * Trigger a manual re-settlement run for a closed business date (D-1 or earlier).
 */

export interface AdminSettlementRunRequest {
  rerun?: boolean;
  target_date: Date;
}

export const AdminSettlementRunRequestSchema = z.object({
  rerun: z.boolean().optional(),
  target_date: z.date(),
});
