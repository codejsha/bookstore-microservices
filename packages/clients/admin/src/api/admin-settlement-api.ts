// Generated by oapi-codegen-cli (1.0.0)

/**
 * Thrown by every AdminSettlementApiClient method when the HTTP response is not 2xx.
 * Carries the status code and the response body parsed per the response's declared
 * content-type (JSON parsed object, text body for `text/*`, raw `Blob` for binary,
 * or `undefined` when the response had no body).
 */
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly statusText: string,
    public readonly body: unknown,
  ) {
    super(`HTTP ${status}: ${statusText}`);
    this.name = 'ApiError';
  }
}
import type { AdminSettlementFindAllResponse } from '../model/admin-settlement-find-all-response';
import type { AdminSettlementResponse } from '../model/admin-settlement-response';
import type { AdminSettlementRunRequest } from '../model/admin-settlement-run-request';
import type { AdminSettlementRunResponse } from '../model/admin-settlement-run-response';

export interface Pageable {
  page?: number;
  size?: number;
  sort?: string;
}

/**
 * Credentials for AdminSettlementApi. One field per declared security scheme;
 * supply the value for the scheme(s) the operation you call requires.
 *   - `api-key` schemes carry the raw key (set as the configured header / query / cookie).
 *   - `http-bearer` / `oauth2` / `openid-connect` carry the token (sent as `Authorization: Bearer …`).
 *   - `http-basic` carries the already-base64-encoded `username:password` (sent as `Authorization: Basic …`).
 */
export interface AdminSettlementApiCredentials {
  /** Scheme `BearerAuth` (`http-bearer`). */
  bearerAuth?: string;
}

/**
 * AdminSettlement API
 */
export interface AdminSettlementApi {

  /**
   * Trigger a manual re-settlement run for a closed business date. Rejected with 400 if a run for that date is already running, the date is already settled (unless rerun=true), or the date is not yet closed.
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   */
  adminSettlementsTriggerRun(
    body: AdminSettlementRunRequest,
  ): Promise<AdminSettlementRunResponse>;

  /**
   * List settlement buckets, optionally filtered by date and status
   * Errors:
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   */
  adminSettlementsListSettlements(
    date: Date | undefined,
    status: string | undefined,
    pageable: Pageable,
  ): Promise<AdminSettlementFindAllResponse>;

  /**
   * Find a settlement bucket with its contributing detail lines
   * Errors:
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  adminSettlementsReadSettlement(
    uid: string,
  ): Promise<AdminSettlementResponse>;
}

export class AdminSettlementApiClient implements AdminSettlementApi {
  private readonly baseUrl: string;
  private readonly credentials: AdminSettlementApiCredentials;

  constructor(baseUrl: string, credentials: AdminSettlementApiCredentials = {}) {
    this.baseUrl = baseUrl;
    this.credentials = credentials;
  }

  async adminSettlementsTriggerRun(
    body: AdminSettlementRunRequest,
  ): Promise<AdminSettlementRunResponse> {
    const url = `${this.baseUrl}/api/v1/admin/settlement-runs`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminSettlementRunResponse;
  }

  async adminSettlementsListSettlements(
    date: Date | undefined,
    status: string | undefined,
    pageable: Pageable,
  ): Promise<AdminSettlementFindAllResponse> {
    const params = new URLSearchParams();
    if (date !== undefined) params.append('date', Array.isArray(date) ? (date as readonly unknown[]).join(',') : String(date));
    if (status !== undefined) params.append('status', Array.isArray(status) ? (status as readonly unknown[]).join(',') : String(status));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/admin/settlements${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminSettlementFindAllResponse;
  }

  async adminSettlementsReadSettlement(
    uid: string,
  ): Promise<AdminSettlementResponse> {
    const url = `${this.baseUrl}/api/v1/admin/settlements/${uid}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminSettlementResponse;
  }
}
