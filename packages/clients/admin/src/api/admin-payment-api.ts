// Generated by oapi-codegen-cli (1.0.0)

/**
 * Thrown by every AdminPaymentApiClient method when the HTTP response is not 2xx.
 * Carries the status code and the response body parsed per the response's declared
 * content-type (JSON parsed object, text body for `text/*`, raw `Blob` for binary,
 * or `undefined` when the response had no body).
 */
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly statusText: string,
    public readonly body: unknown,
  ) {
    super(`HTTP ${status}: ${statusText}`);
    this.name = 'ApiError';
  }
}
import type { AdminPaymentFindAllResponse } from '../model/admin-payment-find-all-response';
import type { AdminPaymentResponse } from '../model/admin-payment-response';
import type { AdminRefundFindAllResponse } from '../model/admin-refund-find-all-response';
import type { AdminRefundResponse } from '../model/admin-refund-response';

export interface Pageable {
  page?: number;
  size?: number;
  sort?: string;
}

/**
 * Credentials for AdminPaymentApi. One field per declared security scheme;
 * supply the value for the scheme(s) the operation you call requires.
 *   - `api-key` schemes carry the raw key (set as the configured header / query / cookie).
 *   - `http-bearer` / `oauth2` / `openid-connect` carry the token (sent as `Authorization: Bearer …`).
 *   - `http-basic` carries the already-base64-encoded `username:password` (sent as `Authorization: Basic …`).
 */
export interface AdminPaymentApiCredentials {
  /** Scheme `BearerAuth` (`http-bearer`). */
  bearerAuth?: string;
}

/**
 * AdminPayment API
 */
export interface AdminPaymentApi {

  /**
   * List payments across all customers. Omit customer_id for the unscoped list.
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   */
  adminPaymentsListPayments(
    customerId: string | undefined,
    status: string | undefined,
    connector: string | undefined,
    pageable: Pageable,
  ): Promise<AdminPaymentFindAllResponse>;

  /**
   * Find a payment by uid
   * Errors:
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  adminPaymentsReadPayment(
    uid: string,
  ): Promise<AdminPaymentResponse>;

  /**
   * List refunds, optionally those raised against one payment
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   */
  adminPaymentsListRefunds(
    paymentId: string | undefined,
    status: string | undefined,
    pageable: Pageable,
  ): Promise<AdminRefundFindAllResponse>;

  /**
   * Find a refund by uid
   * Errors:
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  adminPaymentsReadRefund(
    uid: string,
  ): Promise<AdminRefundResponse>;
}

export class AdminPaymentApiClient implements AdminPaymentApi {
  private readonly baseUrl: string;
  private readonly credentials: AdminPaymentApiCredentials;

  constructor(baseUrl: string, credentials: AdminPaymentApiCredentials = {}) {
    this.baseUrl = baseUrl;
    this.credentials = credentials;
  }

  async adminPaymentsListPayments(
    customerId: string | undefined,
    status: string | undefined,
    connector: string | undefined,
    pageable: Pageable,
  ): Promise<AdminPaymentFindAllResponse> {
    const params = new URLSearchParams();
    if (customerId !== undefined) params.append('customer_id', Array.isArray(customerId) ? (customerId as readonly unknown[]).join(',') : String(customerId));
    if (status !== undefined) params.append('status', Array.isArray(status) ? (status as readonly unknown[]).join(',') : String(status));
    if (connector !== undefined) params.append('connector', Array.isArray(connector) ? (connector as readonly unknown[]).join(',') : String(connector));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/admin/payments${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminPaymentFindAllResponse;
  }

  async adminPaymentsReadPayment(
    uid: string,
  ): Promise<AdminPaymentResponse> {
    const url = `${this.baseUrl}/api/v1/admin/payments/${uid}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminPaymentResponse;
  }

  async adminPaymentsListRefunds(
    paymentId: string | undefined,
    status: string | undefined,
    pageable: Pageable,
  ): Promise<AdminRefundFindAllResponse> {
    const params = new URLSearchParams();
    if (paymentId !== undefined) params.append('payment_id', Array.isArray(paymentId) ? (paymentId as readonly unknown[]).join(',') : String(paymentId));
    if (status !== undefined) params.append('status', Array.isArray(status) ? (status as readonly unknown[]).join(',') : String(status));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/admin/refunds${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminRefundFindAllResponse;
  }

  async adminPaymentsReadRefund(
    uid: string,
  ): Promise<AdminRefundResponse> {
    const url = `${this.baseUrl}/api/v1/admin/refunds/${uid}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminRefundResponse;
  }
}
