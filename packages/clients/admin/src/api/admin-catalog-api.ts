// Generated by oapi-codegen-cli (1.0.0)

/**
 * Thrown by every AdminCatalogApiClient method when the HTTP response is not 2xx.
 * Carries the status code and the response body parsed per the response's declared
 * content-type (JSON parsed object, text body for `text/*`, raw `Blob` for binary,
 * or `undefined` when the response had no body).
 */
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly statusText: string,
    public readonly body: unknown,
  ) {
    super(`HTTP ${status}: ${statusText}`);
    this.name = 'ApiError';
  }
}
import type { AdminAuthorFindAllResponse } from '../model/admin-author-find-all-response';
import type { AdminSubjectFindAllResponse } from '../model/admin-subject-find-all-response';
import type { AdminWorkCreateRequest } from '../model/admin-work-create-request';
import type { AdminWorkFindAllResponse } from '../model/admin-work-find-all-response';
import type { AdminWorkResponse } from '../model/admin-work-response';
import type { AdminWorkUpdateRequest } from '../model/admin-work-update-request';

export interface Pageable {
  page?: number;
  size?: number;
  sort?: string;
}

/**
 * Credentials for AdminCatalogApi. One field per declared security scheme;
 * supply the value for the scheme(s) the operation you call requires.
 *   - `api-key` schemes carry the raw key (set as the configured header / query / cookie).
 *   - `http-bearer` / `oauth2` / `openid-connect` carry the token (sent as `Authorization: Bearer …`).
 *   - `http-basic` carries the already-base64-encoded `username:password` (sent as `Authorization: Basic …`).
 */
export interface AdminCatalogApiCredentials {
  /** Scheme `BearerAuth` (`http-bearer`). */
  bearerAuth?: string;
}

/**
 * AdminCatalog API
 */
export interface AdminCatalogApi {

  /**
   * List authors, for the work form's author picker
   * Errors:
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   */
  adminCatalogListAuthors(
    name: string | undefined,
    pageable: Pageable,
  ): Promise<AdminAuthorFindAllResponse>;

  /**
   * List subjects, for the work form's subject picker
   * Errors:
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   */
  adminCatalogListSubjects(
    name: string | undefined,
    pageable: Pageable,
  ): Promise<AdminSubjectFindAllResponse>;

  /**
   * List works
   * Errors:
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   */
  adminCatalogListWorks(
    title: string | undefined,
    authorUid: string | undefined,
    subjectUid: string | undefined,
    pageable: Pageable,
  ): Promise<AdminWorkFindAllResponse>;

  /**
   * Create a work. The catalog answers 201 with no body, so the new uid is not returned.
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   */
  adminCatalogCreateWork(
    body: AdminWorkCreateRequest,
  ): Promise<void>;

  /**
   * Find a work by uid
   * Errors:
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  adminCatalogReadWork(
    uid: string,
  ): Promise<AdminWorkResponse>;

  /**
   * Update a work
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  adminCatalogUpdateWork(
    uid: string,
    body: AdminWorkUpdateRequest,
  ): Promise<AdminWorkResponse>;
}

export class AdminCatalogApiClient implements AdminCatalogApi {
  private readonly baseUrl: string;
  private readonly credentials: AdminCatalogApiCredentials;

  constructor(baseUrl: string, credentials: AdminCatalogApiCredentials = {}) {
    this.baseUrl = baseUrl;
    this.credentials = credentials;
  }

  async adminCatalogListAuthors(
    name: string | undefined,
    pageable: Pageable,
  ): Promise<AdminAuthorFindAllResponse> {
    const params = new URLSearchParams();
    if (name !== undefined) params.append('name', Array.isArray(name) ? (name as readonly unknown[]).join(',') : String(name));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/admin/catalog/authors${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminAuthorFindAllResponse;
  }

  async adminCatalogListSubjects(
    name: string | undefined,
    pageable: Pageable,
  ): Promise<AdminSubjectFindAllResponse> {
    const params = new URLSearchParams();
    if (name !== undefined) params.append('name', Array.isArray(name) ? (name as readonly unknown[]).join(',') : String(name));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/admin/catalog/subjects${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminSubjectFindAllResponse;
  }

  async adminCatalogListWorks(
    title: string | undefined,
    authorUid: string | undefined,
    subjectUid: string | undefined,
    pageable: Pageable,
  ): Promise<AdminWorkFindAllResponse> {
    const params = new URLSearchParams();
    if (title !== undefined) params.append('title', Array.isArray(title) ? (title as readonly unknown[]).join(',') : String(title));
    if (authorUid !== undefined) params.append('author_uid', Array.isArray(authorUid) ? (authorUid as readonly unknown[]).join(',') : String(authorUid));
    if (subjectUid !== undefined) params.append('subject_uid', Array.isArray(subjectUid) ? (subjectUid as readonly unknown[]).join(',') : String(subjectUid));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/admin/catalog/works${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminWorkFindAllResponse;
  }

  async adminCatalogCreateWork(
    body: AdminWorkCreateRequest,
  ): Promise<void> {
    const url = `${this.baseUrl}/api/v1/admin/catalog/works`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
  }

  async adminCatalogReadWork(
    uid: string,
  ): Promise<AdminWorkResponse> {
    const url = `${this.baseUrl}/api/v1/admin/catalog/works/${uid}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminWorkResponse;
  }

  async adminCatalogUpdateWork(
    uid: string,
    body: AdminWorkUpdateRequest,
  ): Promise<AdminWorkResponse> {
    const url = `${this.baseUrl}/api/v1/admin/catalog/works/${uid}`;
    const response = await fetch(url, {
      method: 'PUT',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminWorkResponse;
  }
}
