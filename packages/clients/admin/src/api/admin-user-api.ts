// Generated by oapi-codegen-cli (1.0.0)

/**
 * Thrown by every AdminUserApiClient method when the HTTP response is not 2xx.
 * Carries the status code and the response body parsed per the response's declared
 * content-type (JSON parsed object, text body for `text/*`, raw `Blob` for binary,
 * or `undefined` when the response had no body).
 */
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly statusText: string,
    public readonly body: unknown,
  ) {
    super(`HTTP ${status}: ${statusText}`);
    this.name = 'ApiError';
  }
}
import type { AdminUserFindAllResponse } from '../model/admin-user-find-all-response';
import type { AdminUserResponse } from '../model/admin-user-response';
import type { AdminUserRolesRequest } from '../model/admin-user-roles-request';

export interface Pageable {
  page?: number;
  size?: number;
  sort?: string;
}

/**
 * Credentials for AdminUserApi. One field per declared security scheme;
 * supply the value for the scheme(s) the operation you call requires.
 *   - `api-key` schemes carry the raw key (set as the configured header / query / cookie).
 *   - `http-bearer` / `oauth2` / `openid-connect` carry the token (sent as `Authorization: Bearer …`).
 *   - `http-basic` carries the already-base64-encoded `username:password` (sent as `Authorization: Basic …`).
 */
export interface AdminUserApiCredentials {
  /** Scheme `BearerAuth` (`http-bearer`). */
  bearerAuth?: string;
}

/**
 * AdminUser API
 */
export interface AdminUserApi {

  /**
   * List user accounts
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   */
  adminUsersListUsers(
    email: string | undefined,
    name: string | undefined,
    phone: string | undefined,
    status: string | undefined,
    pageable: Pageable,
  ): Promise<AdminUserFindAllResponse>;

  /**
   * Find a user by uid
   * Errors:
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  adminUsersReadUser(
    uid: string,
  ): Promise<AdminUserResponse>;

  /**
   * Deactivate an account (deleted in Keycloak, soft-deleted locally). An administrator may not deactivate themselves.
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  adminUsersDeactivateUser(
    uid: string,
  ): Promise<AdminUserResponse>;

  /**
   * Reactivate a suspended account
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  adminUsersReactivateUser(
    uid: string,
  ): Promise<AdminUserResponse>;

  /**
   * Replace a user's roles. An administrator may not change their own roles.
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  adminUsersUpdateUserRoles(
    uid: string,
    body: AdminUserRolesRequest,
  ): Promise<AdminUserResponse>;

  /**
   * Suspend an account (disabled in Keycloak). An administrator may not suspend themselves.
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 401 (UnauthorizedError) — Access is unauthorized.
   * 403 (ForbiddenError) — Access is forbidden.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  adminUsersSuspendUser(
    uid: string,
  ): Promise<AdminUserResponse>;
}

export class AdminUserApiClient implements AdminUserApi {
  private readonly baseUrl: string;
  private readonly credentials: AdminUserApiCredentials;

  constructor(baseUrl: string, credentials: AdminUserApiCredentials = {}) {
    this.baseUrl = baseUrl;
    this.credentials = credentials;
  }

  async adminUsersListUsers(
    email: string | undefined,
    name: string | undefined,
    phone: string | undefined,
    status: string | undefined,
    pageable: Pageable,
  ): Promise<AdminUserFindAllResponse> {
    const params = new URLSearchParams();
    if (email !== undefined) params.append('email', Array.isArray(email) ? (email as readonly unknown[]).join(',') : String(email));
    if (name !== undefined) params.append('name', Array.isArray(name) ? (name as readonly unknown[]).join(',') : String(name));
    if (phone !== undefined) params.append('phone', Array.isArray(phone) ? (phone as readonly unknown[]).join(',') : String(phone));
    if (status !== undefined) params.append('status', Array.isArray(status) ? (status as readonly unknown[]).join(',') : String(status));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/admin/users${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminUserFindAllResponse;
  }

  async adminUsersReadUser(
    uid: string,
  ): Promise<AdminUserResponse> {
    const url = `${this.baseUrl}/api/v1/admin/users/${uid}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminUserResponse;
  }

  async adminUsersDeactivateUser(
    uid: string,
  ): Promise<AdminUserResponse> {
    const url = `${this.baseUrl}/api/v1/admin/users/${uid}/deactivate`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminUserResponse;
  }

  async adminUsersReactivateUser(
    uid: string,
  ): Promise<AdminUserResponse> {
    const url = `${this.baseUrl}/api/v1/admin/users/${uid}/reactivate`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminUserResponse;
  }

  async adminUsersUpdateUserRoles(
    uid: string,
    body: AdminUserRolesRequest,
  ): Promise<AdminUserResponse> {
    const url = `${this.baseUrl}/api/v1/admin/users/${uid}/roles`;
    const response = await fetch(url, {
      method: 'PUT',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminUserResponse;
  }

  async adminUsersSuspendUser(
    uid: string,
  ): Promise<AdminUserResponse> {
    const url = `${this.baseUrl}/api/v1/admin/users/${uid}/suspend`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as AdminUserResponse;
  }
}
