// Generated by oapi-codegen-cli (1.0.0)

/**
 * Thrown by every ShipmentApiClient method when the HTTP response is not 2xx.
 * Carries the status code and the response body parsed per the response's declared
 * content-type (JSON parsed object, text body for `text/*`, raw `Blob` for binary,
 * or `undefined` when the response had no body).
 */
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly statusText: string,
    public readonly body: unknown,
  ) {
    super(`HTTP ${status}: ${statusText}`);
    this.name = 'ApiError';
  }
}
import type { ShipmentAssignCarrierRequest } from '../model/shipment-assign-carrier-request';
import type { ShipmentCreateRequest } from '../model/shipment-create-request';
import type { ShipmentFindAllResponse } from '../model/shipment-find-all-response';
import type { ShipmentFindResponse } from '../model/shipment-find-response';
import type { ShipmentUpdateResponse } from '../model/shipment-update-response';
import type { TrackingCreateRequest } from '../model/tracking-create-request';
import type { TrackingFindAllResponse } from '../model/tracking-find-all-response';
import { ShipmentStatus } from '../constant/shipment-status';

export interface Pageable {
  page?: number;
  size?: number;
  sort?: string;
}

/**
 * Credentials for ShipmentApi. One field per declared security scheme;
 * supply the value for the scheme(s) the operation you call requires.
 *   - `api-key` schemes carry the raw key (set as the configured header / query / cookie).
 *   - `http-bearer` / `oauth2` / `openid-connect` carry the token (sent as `Authorization: Bearer …`).
 *   - `http-basic` carries the already-base64-encoded `username:password` (sent as `Authorization: Basic …`).
 */
export interface ShipmentApiCredentials {
  /** Scheme `BearerAuth` (`http-bearer`). */
  bearerAuth?: string;
}

/**
 * Shipment API
 */
export interface ShipmentApi {

  /**
   * List shipments
   * Errors:
   * 422 (UnprocessableEntityError) — Client error
   */
  shipmentsGetAll(
    orderUid: string | undefined,
    carrierUid: string | undefined,
    status: ShipmentStatus | undefined,
    pageable: Pageable,
  ): Promise<ShipmentFindAllResponse>;

  /**
   * Create shipment
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 409 (ConflictError) — The request conflicts with the current state of the server.
   * 422 (UnprocessableEntityError) — Client error
   *
   * Response headers (on success; read via `response.headers` — not part of the return value):
   * - `location`: string — Location of the created resource
   */
  shipmentsCreate(
    body: ShipmentCreateRequest,
  ): Promise<void>;

  /**
   * Get tracking history
   * Errors:
   * 422 (UnprocessableEntityError) — Client error
   */
  shipmentsGetTrackingHistory(
    shipmentUid: string,
  ): Promise<TrackingFindAllResponse>;

  /**
   * Add tracking event
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   * 422 (UnprocessableEntityError) — Client error
   *
   * Response headers (on success; read via `response.headers` — not part of the return value):
   * - `location`: string — Location of the created resource
   */
  shipmentsAddTracking(
    shipmentUid: string,
    body: TrackingCreateRequest,
  ): Promise<void>;

  /**
   * Find shipment
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   * 422 (UnprocessableEntityError) — Client error
   */
  shipmentsRead(
    uid: string,
  ): Promise<ShipmentFindResponse>;

  /**
   * Cancel shipment
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   * 422 (UnprocessableEntityError) — Client error
   */
  shipmentsCancel(
    uid: string,
  ): Promise<ShipmentUpdateResponse>;

  /**
   * Assign carrier to shipment
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   * 422 (UnprocessableEntityError) — Client error
   */
  shipmentsAssignCarrier(
    uid: string,
    body: ShipmentAssignCarrierRequest,
  ): Promise<ShipmentUpdateResponse>;

  /**
   * Mark shipment as delivered
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   * 422 (UnprocessableEntityError) — Client error
   */
  shipmentsDeliver(
    uid: string,
  ): Promise<ShipmentUpdateResponse>;

  /**
   * Dispatch shipment
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   * 422 (UnprocessableEntityError) — Client error
   */
  shipmentsDispatch(
    uid: string,
  ): Promise<ShipmentUpdateResponse>;

  /**
   * Mark shipment as picked up
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   * 422 (UnprocessableEntityError) — Client error
   */
  shipmentsPickUp(
    uid: string,
  ): Promise<ShipmentUpdateResponse>;
}

export class ShipmentApiClient implements ShipmentApi {
  private readonly baseUrl: string;
  private readonly credentials: ShipmentApiCredentials;

  constructor(baseUrl: string, credentials: ShipmentApiCredentials = {}) {
    this.baseUrl = baseUrl;
    this.credentials = credentials;
  }

  async shipmentsGetAll(
    orderUid: string | undefined,
    carrierUid: string | undefined,
    status: ShipmentStatus | undefined,
    pageable: Pageable,
  ): Promise<ShipmentFindAllResponse> {
    const params = new URLSearchParams();
    if (orderUid !== undefined) params.append('order_uid', Array.isArray(orderUid) ? (orderUid as readonly unknown[]).join(',') : String(orderUid));
    if (carrierUid !== undefined) params.append('carrier_uid', Array.isArray(carrierUid) ? (carrierUid as readonly unknown[]).join(',') : String(carrierUid));
    if (status !== undefined) params.append('status', Array.isArray(status) ? (status as readonly unknown[]).join(',') : String(status));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/shipments${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as ShipmentFindAllResponse;
  }

  async shipmentsCreate(
    body: ShipmentCreateRequest,
  ): Promise<void> {
    const url = `${this.baseUrl}/api/v1/shipments`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
  }

  async shipmentsGetTrackingHistory(
    shipmentUid: string,
  ): Promise<TrackingFindAllResponse> {
    const url = `${this.baseUrl}/api/v1/shipments/${shipmentUid}/tracking`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as TrackingFindAllResponse;
  }

  async shipmentsAddTracking(
    shipmentUid: string,
    body: TrackingCreateRequest,
  ): Promise<void> {
    const url = `${this.baseUrl}/api/v1/shipments/${shipmentUid}/tracking`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
  }

  async shipmentsRead(
    uid: string,
  ): Promise<ShipmentFindResponse> {
    const url = `${this.baseUrl}/api/v1/shipments/${uid}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as ShipmentFindResponse;
  }

  async shipmentsCancel(
    uid: string,
  ): Promise<ShipmentUpdateResponse> {
    const url = `${this.baseUrl}/api/v1/shipments/${uid}/cancel`;
    const response = await fetch(url, {
      method: 'PATCH',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as ShipmentUpdateResponse;
  }

  async shipmentsAssignCarrier(
    uid: string,
    body: ShipmentAssignCarrierRequest,
  ): Promise<ShipmentUpdateResponse> {
    const url = `${this.baseUrl}/api/v1/shipments/${uid}/carrier`;
    const response = await fetch(url, {
      method: 'PATCH',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as ShipmentUpdateResponse;
  }

  async shipmentsDeliver(
    uid: string,
  ): Promise<ShipmentUpdateResponse> {
    const url = `${this.baseUrl}/api/v1/shipments/${uid}/deliver`;
    const response = await fetch(url, {
      method: 'PATCH',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as ShipmentUpdateResponse;
  }

  async shipmentsDispatch(
    uid: string,
  ): Promise<ShipmentUpdateResponse> {
    const url = `${this.baseUrl}/api/v1/shipments/${uid}/dispatch`;
    const response = await fetch(url, {
      method: 'PATCH',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as ShipmentUpdateResponse;
  }

  async shipmentsPickUp(
    uid: string,
  ): Promise<ShipmentUpdateResponse> {
    const url = `${this.baseUrl}/api/v1/shipments/${uid}/pickup`;
    const response = await fetch(url, {
      method: 'PATCH',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as ShipmentUpdateResponse;
  }
}
