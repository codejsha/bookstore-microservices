// Generated by oapi-codegen-cli (1.0.0)

/**
 * Thrown by every CarrierApiClient method when the HTTP response is not 2xx.
 * Carries the status code and the response body parsed per the response's declared
 * content-type (JSON parsed object, text body for `text/*`, raw `Blob` for binary,
 * or `undefined` when the response had no body).
 */
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly statusText: string,
    public readonly body: unknown,
  ) {
    super(`HTTP ${status}: ${statusText}`);
    this.name = 'ApiError';
  }
}
import type { CarrierCreateRequest } from '../model/carrier-create-request';
import type { CarrierFindAllResponse } from '../model/carrier-find-all-response';
import type { CarrierFindResponse } from '../model/carrier-find-response';
import type { CarrierUpdateRequest } from '../model/carrier-update-request';
import type { CarrierUpdateResponse } from '../model/carrier-update-response';
import { CarrierStatus } from '../constant/carrier-status';

export interface Pageable {
  page?: number;
  size?: number;
  sort?: string;
}

/**
 * Credentials for CarrierApi. One field per declared security scheme;
 * supply the value for the scheme(s) the operation you call requires.
 *   - `api-key` schemes carry the raw key (set as the configured header / query / cookie).
 *   - `http-bearer` / `oauth2` / `openid-connect` carry the token (sent as `Authorization: Bearer …`).
 *   - `http-basic` carries the already-base64-encoded `username:password` (sent as `Authorization: Basic …`).
 */
export interface CarrierApiCredentials {
  /** Scheme `BearerAuth` (`http-bearer`). */
  bearerAuth?: string;
}

/**
 * Carrier API
 */
export interface CarrierApi {

  /**
   * List carriers
   */
  carriersGetAll(
    name: string | undefined,
    status: CarrierStatus | undefined,
    pageable: Pageable,
  ): Promise<CarrierFindAllResponse>;

  /**
   * Register new carrier
   *
   * Response headers (on success; read via `response.headers` — not part of the return value):
   * - `location`: string — Location of the created resource
   */
  carriersCreate(
    body: CarrierCreateRequest,
  ): Promise<void>;

  /**
   * Find carrier
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  carriersRead(
    uid: string,
  ): Promise<CarrierFindResponse>;

  /**
   * Update carrier
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  carriersUpdate(
    uid: string,
    body: CarrierUpdateRequest,
  ): Promise<CarrierUpdateResponse>;
}

export class CarrierApiClient implements CarrierApi {
  private readonly baseUrl: string;
  private readonly credentials: CarrierApiCredentials;

  constructor(baseUrl: string, credentials: CarrierApiCredentials = {}) {
    this.baseUrl = baseUrl;
    this.credentials = credentials;
  }

  async carriersGetAll(
    name: string | undefined,
    status: CarrierStatus | undefined,
    pageable: Pageable,
  ): Promise<CarrierFindAllResponse> {
    const params = new URLSearchParams();
    if (name !== undefined) params.append('name', Array.isArray(name) ? (name as readonly unknown[]).join(',') : String(name));
    if (status !== undefined) params.append('status', Array.isArray(status) ? (status as readonly unknown[]).join(',') : String(status));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/carriers${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as CarrierFindAllResponse;
  }

  async carriersCreate(
    body: CarrierCreateRequest,
  ): Promise<void> {
    const url = `${this.baseUrl}/api/v1/carriers`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
  }

  async carriersRead(
    uid: string,
  ): Promise<CarrierFindResponse> {
    const url = `${this.baseUrl}/api/v1/carriers/${uid}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as CarrierFindResponse;
  }

  async carriersUpdate(
    uid: string,
    body: CarrierUpdateRequest,
  ): Promise<CarrierUpdateResponse> {
    const url = `${this.baseUrl}/api/v1/carriers/${uid}`;
    const response = await fetch(url, {
      method: 'PUT',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as CarrierUpdateResponse;
  }
}
