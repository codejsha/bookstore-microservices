// Generated by oapi-codegen-cli (1.0.0)

import { z } from 'zod';

export enum FreightStatus {
  FreightStatusEstimated = 'ESTIMATED',
  FreightStatusConfirmed = 'CONFIRMED',
  FreightStatusInvoiced = 'INVOICED',
  FreightStatusPaid = 'PAID',
}

export const FreightStatusSchema = z.enum([
  'ESTIMATED',
  'CONFIRMED',
  'INVOICED',
  'PAID',
]);

/**
 * Parses an OpenAPI string value into a {@link FreightStatus}.
 * @throws {Error} when the value is not a known FreightStatus.
 */
export function parseFreightStatus(value: string): FreightStatus {
  if (isFreightStatus(value)) {
    return value;
  }
  throw new Error(`Invalid FreightStatus value: ${value}`);
}

/** Type guard returning whether `value` is a known {@link FreightStatus}. */
export function isFreightStatus(value: string): value is FreightStatus {
  return (Object.values(FreightStatus) as string[]).includes(value);
}
