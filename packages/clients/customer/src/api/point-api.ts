// Generated by oapi-codegen-cli (1.0.0)

/**
 * Thrown by every PointApiClient method when the HTTP response is not 2xx.
 * Carries the status code and the response body parsed per the response's declared
 * content-type (JSON parsed object, text body for `text/*`, raw `Blob` for binary,
 * or `undefined` when the response had no body).
 */
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly statusText: string,
    public readonly body: unknown,
  ) {
    super(`HTTP ${status}: ${statusText}`);
    this.name = 'ApiError';
  }
}
import type { PointBalanceResponse } from '../model/point-balance-response';
import type { PointEarnRequest } from '../model/point-earn-request';
import type { PointHistoryFindAllResponse } from '../model/point-history-find-all-response';
import type { PointSpendRequest } from '../model/point-spend-request';

export interface Pageable {
  page?: number;
  size?: number;
  sort?: string;
}

/**
 * Credentials for PointApi. One field per declared security scheme;
 * supply the value for the scheme(s) the operation you call requires.
 *   - `api-key` schemes carry the raw key (set as the configured header / query / cookie).
 *   - `http-bearer` / `oauth2` / `openid-connect` carry the token (sent as `Authorization: Bearer …`).
 *   - `http-basic` carries the already-base64-encoded `username:password` (sent as `Authorization: Basic …`).
 */
export interface PointApiCredentials {
  /** Scheme `BearerAuth` (`http-bearer`). */
  bearerAuth?: string;
}

/**
 * Point API
 */
export interface PointApi {

  /**
   * Get customer's point balance
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  pointsGetBalance(
    uid: string,
  ): Promise<PointBalanceResponse>;

  /**
   * Earn points for a customer
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  pointsEarn(
    uid: string,
    body: PointEarnRequest,
  ): Promise<PointBalanceResponse>;

  /**
   * Get point change history
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  pointsHistory(
    uid: string,
    pageable: Pageable,
  ): Promise<PointHistoryFindAllResponse>;

  /**
   * Spend points for a customer
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  pointsSpend(
    uid: string,
    body: PointSpendRequest,
  ): Promise<PointBalanceResponse>;
}

export class PointApiClient implements PointApi {
  private readonly baseUrl: string;
  private readonly credentials: PointApiCredentials;

  constructor(baseUrl: string, credentials: PointApiCredentials = {}) {
    this.baseUrl = baseUrl;
    this.credentials = credentials;
  }

  async pointsGetBalance(
    uid: string,
  ): Promise<PointBalanceResponse> {
    const url = `${this.baseUrl}/api/v1/customers/${uid}/points`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as PointBalanceResponse;
  }

  async pointsEarn(
    uid: string,
    body: PointEarnRequest,
  ): Promise<PointBalanceResponse> {
    const url = `${this.baseUrl}/api/v1/customers/${uid}/points/earn`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as PointBalanceResponse;
  }

  async pointsHistory(
    uid: string,
    pageable: Pageable,
  ): Promise<PointHistoryFindAllResponse> {
    const params = new URLSearchParams();
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/customers/${uid}/points/history${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as PointHistoryFindAllResponse;
  }

  async pointsSpend(
    uid: string,
    body: PointSpendRequest,
  ): Promise<PointBalanceResponse> {
    const url = `${this.baseUrl}/api/v1/customers/${uid}/points/spend`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as PointBalanceResponse;
  }
}
