// Generated by oapi-codegen-cli (1.0.0)

/**
 * Thrown by every ReviewApiClient method when the HTTP response is not 2xx.
 * Carries the status code and the response body parsed per the response's declared
 * content-type (JSON parsed object, text body for `text/*`, raw `Blob` for binary,
 * or `undefined` when the response had no body).
 */
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly statusText: string,
    public readonly body: unknown,
  ) {
    super(`HTTP ${status}: ${statusText}`);
    this.name = 'ApiError';
  }
}
import type { ReviewCreateRequest } from '../model/review-create-request';
import type { ReviewFindAllResponse } from '../model/review-find-all-response';
import type { ReviewFindResponse } from '../model/review-find-response';
import type { ReviewUpdateRequest } from '../model/review-update-request';

export interface Pageable {
  page?: number;
  size?: number;
  sort?: string;
}

/**
 * Credentials for ReviewApi. One field per declared security scheme;
 * supply the value for the scheme(s) the operation you call requires.
 *   - `api-key` schemes carry the raw key (set as the configured header / query / cookie).
 *   - `http-bearer` / `oauth2` / `openid-connect` carry the token (sent as `Authorization: Bearer …`).
 *   - `http-basic` carries the already-base64-encoded `username:password` (sent as `Authorization: Basic …`).
 */
export interface ReviewApiCredentials {
  /** Scheme `BearerAuth` (`http-bearer`). */
  bearerAuth?: string;
}

/**
 * Review API
 */
export interface ReviewApi {

  /**
   * Get all reviews for a book
   */
  bookReviewsGetAll(
    bookUid: string,
    rating: number | undefined,
    pageable: Pageable,
  ): Promise<ReviewFindAllResponse>;

  /**
   * Get all reviews by a customer
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  customerReviewsGetAll(
    uid: string,
    pageable: Pageable,
  ): Promise<ReviewFindAllResponse>;

  /**
   * Write a review for a book
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   *
   * Response headers (on success; read via `response.headers` — not part of the return value):
   * - `location`: string — Location of the created resource
   */
  customerReviewsWrite(
    uid: string,
    body: ReviewCreateRequest,
  ): Promise<void>;

  /**
   * Get a specific review
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  customerReviewsRead(
    uid: string,
    reviewUid: string,
  ): Promise<ReviewFindResponse>;

  /**
   * Edit a review
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  customerReviewsEdit(
    uid: string,
    reviewUid: string,
    body: ReviewUpdateRequest,
  ): Promise<ReviewFindResponse>;

  /**
   * Delete a review
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  customerReviewsRemove(
    uid: string,
    reviewUid: string,
  ): Promise<void>;
}

export class ReviewApiClient implements ReviewApi {
  private readonly baseUrl: string;
  private readonly credentials: ReviewApiCredentials;

  constructor(baseUrl: string, credentials: ReviewApiCredentials = {}) {
    this.baseUrl = baseUrl;
    this.credentials = credentials;
  }

  async bookReviewsGetAll(
    bookUid: string,
    rating: number | undefined,
    pageable: Pageable,
  ): Promise<ReviewFindAllResponse> {
    const params = new URLSearchParams();
    if (rating !== undefined) params.append('rating', Array.isArray(rating) ? (rating as readonly unknown[]).join(',') : String(rating));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/books/${bookUid}/reviews${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as ReviewFindAllResponse;
  }

  async customerReviewsGetAll(
    uid: string,
    pageable: Pageable,
  ): Promise<ReviewFindAllResponse> {
    const params = new URLSearchParams();
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/customers/${uid}/reviews${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as ReviewFindAllResponse;
  }

  async customerReviewsWrite(
    uid: string,
    body: ReviewCreateRequest,
  ): Promise<void> {
    const url = `${this.baseUrl}/api/v1/customers/${uid}/reviews`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
  }

  async customerReviewsRead(
    uid: string,
    reviewUid: string,
  ): Promise<ReviewFindResponse> {
    const url = `${this.baseUrl}/api/v1/customers/${uid}/reviews/${reviewUid}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as ReviewFindResponse;
  }

  async customerReviewsEdit(
    uid: string,
    reviewUid: string,
    body: ReviewUpdateRequest,
  ): Promise<ReviewFindResponse> {
    const url = `${this.baseUrl}/api/v1/customers/${uid}/reviews/${reviewUid}`;
    const response = await fetch(url, {
      method: 'PUT',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as ReviewFindResponse;
  }

  async customerReviewsRemove(
    uid: string,
    reviewUid: string,
  ): Promise<void> {
    const url = `${this.baseUrl}/api/v1/customers/${uid}/reviews/${reviewUid}`;
    const response = await fetch(url, {
      method: 'DELETE',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
  }
}
