// Generated by oapi-codegen-cli (1.0.0)

import { z } from 'zod';
import { RiskLevel, RiskLevelSchema } from '../constant/risk-level';

export interface RiskFlagRequest {
  level: RiskLevel;
  reason: string;
  /** Time-to-live in seconds; defaults to 86400 (24h), capped at 30 days */
  ttl_seconds?: string;
}

export const RiskFlagRequestSchema = z.object({
  level: RiskLevelSchema,
  reason: z.string(),
  ttl_seconds: z.string().regex(/^-?\d+$/).optional(),
});
