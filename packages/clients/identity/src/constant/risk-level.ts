// Generated by oapi-codegen-cli (1.0.0)

import { z } from 'zod';

/**
 * Enforcement level for a flagged principal
 */

export enum RiskLevel {
  RiskLevelRestrict = 'restrict',
  RiskLevelBlock = 'block',
}

export const RiskLevelSchema = z.enum([
  'restrict',
  'block',
]);

/**
 * Parses an OpenAPI string value into a {@link RiskLevel}.
 * @throws {Error} when the value is not a known RiskLevel.
 */
export function parseRiskLevel(value: string): RiskLevel {
  if (isRiskLevel(value)) {
    return value;
  }
  throw new Error(`Invalid RiskLevel value: ${value}`);
}

/** Type guard returning whether `value` is a known {@link RiskLevel}. */
export function isRiskLevel(value: string): value is RiskLevel {
  return (Object.values(RiskLevel) as string[]).includes(value);
}
