// Generated by oapi-codegen-cli (1.0.0)

import { z } from 'zod';

export enum Versions {
  Versions100 = '1.0.0',
}

export const VersionsSchema = z.enum([
  '1.0.0',
]);

/**
 * Parses an OpenAPI string value into a {@link Versions}.
 * @throws {Error} when the value is not a known Versions.
 */
export function parseVersions(value: string): Versions {
  if (isVersions(value)) {
    return value;
  }
  throw new Error(`Invalid Versions value: ${value}`);
}

/** Type guard returning whether `value` is a known {@link Versions}. */
export function isVersions(value: string): value is Versions {
  return (Object.values(Versions) as string[]).includes(value);
}
