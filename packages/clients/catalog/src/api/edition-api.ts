// Generated by oapi-codegen-cli (1.0.0)

/**
 * Thrown by every EditionApiClient method when the HTTP response is not 2xx.
 * Carries the status code and the response body parsed per the response's declared
 * content-type (JSON parsed object, text body for `text/*`, raw `Blob` for binary,
 * or `undefined` when the response had no body).
 */
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly statusText: string,
    public readonly body: unknown,
  ) {
    super(`HTTP ${status}: ${statusText}`);
    this.name = 'ApiError';
  }
}
import type { EditionCreateRequest } from '../model/edition-create-request';
import type { EditionFindAllResponse } from '../model/edition-find-all-response';
import type { EditionFindResponse } from '../model/edition-find-response';
import type { EditionUpdateRequest } from '../model/edition-update-request';
import type { EditionUpdateResponse } from '../model/edition-update-response';

export interface Pageable {
  page?: number;
  size?: number;
  sort?: string;
}

/**
 * Credentials for EditionApi. One field per declared security scheme;
 * supply the value for the scheme(s) the operation you call requires.
 *   - `api-key` schemes carry the raw key (set as the configured header / query / cookie).
 *   - `http-bearer` / `oauth2` / `openid-connect` carry the token (sent as `Authorization: Bearer …`).
 *   - `http-basic` carries the already-base64-encoded `username:password` (sent as `Authorization: Basic …`).
 */
export interface EditionApiCredentials {
  /** Scheme `BearerAuth` (`http-bearer`). */
  bearerAuth?: string;
}

/**
 * Edition API
 */
export interface EditionApi {

  /**
   * Search editions
   */
  editionsSearch(
    title: string | undefined,
    isbn: string | undefined,
    workUid: string | undefined,
    publisherUid: string | undefined,
    language: string | undefined,
    olKey: string | undefined,
    pageable: Pageable,
  ): Promise<EditionFindAllResponse>;

  /**
   * Create new edition
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   * 409 (ConflictError) — The request conflicts with the current state of the server.
   *
   * Response headers (on success; read via `response.headers` — not part of the return value):
   * - `location`: string — Location of the created resource
   */
  editionsCreate(
    body: EditionCreateRequest,
  ): Promise<void>;

  /**
   * Find edition with full details
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  editionsRead(
    uid: string,
  ): Promise<EditionFindResponse>;

  /**
   * Update edition
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   * 409 (ConflictError) — The request conflicts with the current state of the server.
   */
  editionsUpdate(
    uid: string,
    body: EditionUpdateRequest,
  ): Promise<EditionUpdateResponse>;
}

export class EditionApiClient implements EditionApi {
  private readonly baseUrl: string;
  private readonly credentials: EditionApiCredentials;

  constructor(baseUrl: string, credentials: EditionApiCredentials = {}) {
    this.baseUrl = baseUrl;
    this.credentials = credentials;
  }

  async editionsSearch(
    title: string | undefined,
    isbn: string | undefined,
    workUid: string | undefined,
    publisherUid: string | undefined,
    language: string | undefined,
    olKey: string | undefined,
    pageable: Pageable,
  ): Promise<EditionFindAllResponse> {
    const params = new URLSearchParams();
    if (title !== undefined) params.append('title', Array.isArray(title) ? (title as readonly unknown[]).join(',') : String(title));
    if (isbn !== undefined) params.append('isbn', Array.isArray(isbn) ? (isbn as readonly unknown[]).join(',') : String(isbn));
    if (workUid !== undefined) params.append('work_uid', Array.isArray(workUid) ? (workUid as readonly unknown[]).join(',') : String(workUid));
    if (publisherUid !== undefined) params.append('publisher_uid', Array.isArray(publisherUid) ? (publisherUid as readonly unknown[]).join(',') : String(publisherUid));
    if (language !== undefined) params.append('language', Array.isArray(language) ? (language as readonly unknown[]).join(',') : String(language));
    if (olKey !== undefined) params.append('ol_key', Array.isArray(olKey) ? (olKey as readonly unknown[]).join(',') : String(olKey));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/editions${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as EditionFindAllResponse;
  }

  async editionsCreate(
    body: EditionCreateRequest,
  ): Promise<void> {
    const url = `${this.baseUrl}/api/v1/editions`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
  }

  async editionsRead(
    uid: string,
  ): Promise<EditionFindResponse> {
    const url = `${this.baseUrl}/api/v1/editions/${uid}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as EditionFindResponse;
  }

  async editionsUpdate(
    uid: string,
    body: EditionUpdateRequest,
  ): Promise<EditionUpdateResponse> {
    const url = `${this.baseUrl}/api/v1/editions/${uid}`;
    const response = await fetch(url, {
      method: 'PUT',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as EditionUpdateResponse;
  }
}
