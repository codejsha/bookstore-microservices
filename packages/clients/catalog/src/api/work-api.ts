// Generated by oapi-codegen-cli (1.0.0)

/**
 * Thrown by every WorkApiClient method when the HTTP response is not 2xx.
 * Carries the status code and the response body parsed per the response's declared
 * content-type (JSON parsed object, text body for `text/*`, raw `Blob` for binary,
 * or `undefined` when the response had no body).
 */
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly statusText: string,
    public readonly body: unknown,
  ) {
    super(`HTTP ${status}: ${statusText}`);
    this.name = 'ApiError';
  }
}
import type { EditionFindAllResponse } from '../model/edition-find-all-response';
import type { WorkCreateRequest } from '../model/work-create-request';
import type { WorkFindAllResponse } from '../model/work-find-all-response';
import type { WorkFindResponse } from '../model/work-find-response';
import type { WorkSearchResponse } from '../model/work-search-response';
import type { WorkUpdateRequest } from '../model/work-update-request';
import type { WorkUpdateResponse } from '../model/work-update-response';

export interface Pageable {
  page?: number;
  size?: number;
  sort?: string;
}

/**
 * Credentials for WorkApi. One field per declared security scheme;
 * supply the value for the scheme(s) the operation you call requires.
 *   - `api-key` schemes carry the raw key (set as the configured header / query / cookie).
 *   - `http-bearer` / `oauth2` / `openid-connect` carry the token (sent as `Authorization: Bearer …`).
 *   - `http-basic` carries the already-base64-encoded `username:password` (sent as `Authorization: Basic …`).
 */
export interface WorkApiCredentials {
  /** Scheme `BearerAuth` (`http-bearer`). */
  bearerAuth?: string;
}

/**
 * Work API
 */
export interface WorkApi {

  /**
   * Search works
   */
  worksSearch(
    title: string | undefined,
    authorUid: string | undefined,
    subjectUid: string | undefined,
    olKey: string | undefined,
    pageable: Pageable,
  ): Promise<WorkFindAllResponse>;

  /**
   * Create new work
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   * 409 (ConflictError) — The request conflicts with the current state of the server.
   *
   * Response headers (on success; read via `response.headers` — not part of the return value):
   * - `location`: string — Location of the created resource
   */
  worksCreate(
    body: WorkCreateRequest,
  ): Promise<void>;

  /**
   * Full-text search backed by OpenSearch
   */
  worksFullTextSearch(
    q: string | undefined,
    authorUid: string | undefined,
    subjectUid: string | undefined,
    pageable: Pageable,
  ): Promise<WorkSearchResponse>;

  /**
   * Find work with full details
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  worksRead(
    uid: string,
  ): Promise<WorkFindResponse>;

  /**
   * Update work
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   * 409 (ConflictError) — The request conflicts with the current state of the server.
   */
  worksUpdate(
    uid: string,
    body: WorkUpdateRequest,
  ): Promise<WorkUpdateResponse>;

  /**
   * Find editions of a work
   */
  worksEditions(
    uid: string,
    pageable: Pageable,
  ): Promise<EditionFindAllResponse>;
}

export class WorkApiClient implements WorkApi {
  private readonly baseUrl: string;
  private readonly credentials: WorkApiCredentials;

  constructor(baseUrl: string, credentials: WorkApiCredentials = {}) {
    this.baseUrl = baseUrl;
    this.credentials = credentials;
  }

  async worksSearch(
    title: string | undefined,
    authorUid: string | undefined,
    subjectUid: string | undefined,
    olKey: string | undefined,
    pageable: Pageable,
  ): Promise<WorkFindAllResponse> {
    const params = new URLSearchParams();
    if (title !== undefined) params.append('title', Array.isArray(title) ? (title as readonly unknown[]).join(',') : String(title));
    if (authorUid !== undefined) params.append('author_uid', Array.isArray(authorUid) ? (authorUid as readonly unknown[]).join(',') : String(authorUid));
    if (subjectUid !== undefined) params.append('subject_uid', Array.isArray(subjectUid) ? (subjectUid as readonly unknown[]).join(',') : String(subjectUid));
    if (olKey !== undefined) params.append('ol_key', Array.isArray(olKey) ? (olKey as readonly unknown[]).join(',') : String(olKey));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/works${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as WorkFindAllResponse;
  }

  async worksCreate(
    body: WorkCreateRequest,
  ): Promise<void> {
    const url = `${this.baseUrl}/api/v1/works`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
  }

  async worksFullTextSearch(
    q: string | undefined,
    authorUid: string | undefined,
    subjectUid: string | undefined,
    pageable: Pageable,
  ): Promise<WorkSearchResponse> {
    const params = new URLSearchParams();
    if (q !== undefined) params.append('q', Array.isArray(q) ? (q as readonly unknown[]).join(',') : String(q));
    if (authorUid !== undefined) params.append('author_uid', Array.isArray(authorUid) ? (authorUid as readonly unknown[]).join(',') : String(authorUid));
    if (subjectUid !== undefined) params.append('subject_uid', Array.isArray(subjectUid) ? (subjectUid as readonly unknown[]).join(',') : String(subjectUid));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/works/search${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as WorkSearchResponse;
  }

  async worksRead(
    uid: string,
  ): Promise<WorkFindResponse> {
    const url = `${this.baseUrl}/api/v1/works/${uid}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as WorkFindResponse;
  }

  async worksUpdate(
    uid: string,
    body: WorkUpdateRequest,
  ): Promise<WorkUpdateResponse> {
    const url = `${this.baseUrl}/api/v1/works/${uid}`;
    const response = await fetch(url, {
      method: 'PUT',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as WorkUpdateResponse;
  }

  async worksEditions(
    uid: string,
    pageable: Pageable,
  ): Promise<EditionFindAllResponse> {
    const params = new URLSearchParams();
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/works/${uid}/editions${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as EditionFindAllResponse;
  }
}
