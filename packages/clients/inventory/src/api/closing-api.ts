// Generated by oapi-codegen-cli (1.0.0)

/**
 * Thrown by every ClosingApiClient method when the HTTP response is not 2xx.
 * Carries the status code and the response body parsed per the response's declared
 * content-type (JSON parsed object, text body for `text/*`, raw `Blob` for binary,
 * or `undefined` when the response had no body).
 */
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly statusText: string,
    public readonly body: unknown,
  ) {
    super(`HTTP ${status}: ${statusText}`);
    this.name = 'ApiError';
  }
}
import type { ClosingCreateRequest } from '../model/closing-create-request';
import type { ClosingFindAllResponse } from '../model/closing-find-all-response';
import type { ClosingFindResponse } from '../model/closing-find-response';
import { ClosingStatus } from '../constant/closing-status';

export interface Pageable {
  page?: number;
  size?: number;
  sort?: string;
}

/**
 * Credentials for ClosingApi. One field per declared security scheme;
 * supply the value for the scheme(s) the operation you call requires.
 *   - `api-key` schemes carry the raw key (set as the configured header / query / cookie).
 *   - `http-bearer` / `oauth2` / `openid-connect` carry the token (sent as `Authorization: Bearer …`).
 *   - `http-basic` carries the already-base64-encoded `username:password` (sent as `Authorization: Basic …`).
 */
export interface ClosingApiCredentials {
  /** Scheme `BearerAuth` (`http-bearer`). */
  bearerAuth?: string;
}

/**
 * Closing API
 */
export interface ClosingApi {

  /**
   * Find all monthly closings
   */
  closingsGetAll(
    warehouseUid: string | undefined,
    year: number | undefined,
    month: number | undefined,
    status: ClosingStatus | undefined,
    pageable: Pageable,
  ): Promise<ClosingFindAllResponse>;

  /**
   * Create monthly closing (월 마감)
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  closingsCreate(
    body: ClosingCreateRequest,
  ): Promise<ClosingFindResponse>;

  /**
   * Find monthly closing
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  closingsRead(
    uid: string,
  ): Promise<ClosingFindResponse>;
}

export class ClosingApiClient implements ClosingApi {
  private readonly baseUrl: string;
  private readonly credentials: ClosingApiCredentials;

  constructor(baseUrl: string, credentials: ClosingApiCredentials = {}) {
    this.baseUrl = baseUrl;
    this.credentials = credentials;
  }

  async closingsGetAll(
    warehouseUid: string | undefined,
    year: number | undefined,
    month: number | undefined,
    status: ClosingStatus | undefined,
    pageable: Pageable,
  ): Promise<ClosingFindAllResponse> {
    const params = new URLSearchParams();
    if (warehouseUid !== undefined) params.append('warehouse_uid', Array.isArray(warehouseUid) ? (warehouseUid as readonly unknown[]).join(',') : String(warehouseUid));
    if (year !== undefined) params.append('year', Array.isArray(year) ? (year as readonly unknown[]).join(',') : String(year));
    if (month !== undefined) params.append('month', Array.isArray(month) ? (month as readonly unknown[]).join(',') : String(month));
    if (status !== undefined) params.append('status', Array.isArray(status) ? (status as readonly unknown[]).join(',') : String(status));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/closings${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as ClosingFindAllResponse;
  }

  async closingsCreate(
    body: ClosingCreateRequest,
  ): Promise<ClosingFindResponse> {
    const url = `${this.baseUrl}/api/v1/closings`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as ClosingFindResponse;
  }

  async closingsRead(
    uid: string,
  ): Promise<ClosingFindResponse> {
    const url = `${this.baseUrl}/api/v1/closings/${uid}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as ClosingFindResponse;
  }
}
