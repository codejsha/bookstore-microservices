// Generated by oapi-codegen-cli (1.0.0)

/**
 * Thrown by every StockApiClient method when the HTTP response is not 2xx.
 * Carries the status code and the response body parsed per the response's declared
 * content-type (JSON parsed object, text body for `text/*`, raw `Blob` for binary,
 * or `undefined` when the response had no body).
 */
export class ApiError extends Error {
  constructor(
    public readonly status: number,
    public readonly statusText: string,
    public readonly body: unknown,
  ) {
    super(`HTTP ${status}: ${statusText}`);
    this.name = 'ApiError';
  }
}
import type { StockAdjustRequest } from '../model/stock-adjust-request';
import type { StockFindAllResponse } from '../model/stock-find-all-response';
import type { StockFindResponse } from '../model/stock-find-response';
import type { StockHistoryFindAllResponse } from '../model/stock-history-find-all-response';
import type { StockReceiveRequest } from '../model/stock-receive-request';
import type { StockReleaseRequest } from '../model/stock-release-request';
import type { StockReserveRequest } from '../model/stock-reserve-request';

export interface Pageable {
  page?: number;
  size?: number;
  sort?: string;
}

/**
 * Credentials for StockApi. One field per declared security scheme;
 * supply the value for the scheme(s) the operation you call requires.
 *   - `api-key` schemes carry the raw key (set as the configured header / query / cookie).
 *   - `http-bearer` / `oauth2` / `openid-connect` carry the token (sent as `Authorization: Bearer …`).
 *   - `http-basic` carries the already-base64-encoded `username:password` (sent as `Authorization: Basic …`).
 */
export interface StockApiCredentials {
  /** Scheme `BearerAuth` (`http-bearer`). */
  bearerAuth?: string;
}

/**
 * Stock API
 */
export interface StockApi {

  /**
   * Find all stocks grouped by edition
   */
  stocksGetAll(
    editionUid: string | undefined,
    warehouseUid: string | undefined,
    pageable: Pageable,
  ): Promise<StockFindAllResponse>;

  /**
   * Adjust stock quantity (correction)
   */
  stocksAdjust(
    body: StockAdjustRequest,
  ): Promise<StockFindResponse>;

  /**
   * Receive stock into warehouse (inbound)
   */
  stocksReceive(
    body: StockReceiveRequest,
  ): Promise<StockFindResponse>;

  /**
   * Release stock from warehouse (outbound)
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   */
  stocksRelease(
    body: StockReleaseRequest,
  ): Promise<StockFindResponse>;

  /**
   * Reserve stock for an order
   * Errors:
   * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
   */
  stocksReserve(
    body: StockReserveRequest,
  ): Promise<StockFindResponse>;

  /**
   * Find stock by edition UID
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  stocksRead(
    editionUid: string,
  ): Promise<StockFindResponse>;

  /**
   * Find stock change history
   * Errors:
   * 404 (NotFoundError) — The server cannot find the requested resource.
   */
  stocksHistory(
    editionUid: string,
    warehouseUid: string | undefined,
    pageable: Pageable,
  ): Promise<StockHistoryFindAllResponse>;
}

export class StockApiClient implements StockApi {
  private readonly baseUrl: string;
  private readonly credentials: StockApiCredentials;

  constructor(baseUrl: string, credentials: StockApiCredentials = {}) {
    this.baseUrl = baseUrl;
    this.credentials = credentials;
  }

  async stocksGetAll(
    editionUid: string | undefined,
    warehouseUid: string | undefined,
    pageable: Pageable,
  ): Promise<StockFindAllResponse> {
    const params = new URLSearchParams();
    if (editionUid !== undefined) params.append('edition_uid', Array.isArray(editionUid) ? (editionUid as readonly unknown[]).join(',') : String(editionUid));
    if (warehouseUid !== undefined) params.append('warehouse_uid', Array.isArray(warehouseUid) ? (warehouseUid as readonly unknown[]).join(',') : String(warehouseUid));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/stocks${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as StockFindAllResponse;
  }

  async stocksAdjust(
    body: StockAdjustRequest,
  ): Promise<StockFindResponse> {
    const url = `${this.baseUrl}/api/v1/stocks/adjust`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as StockFindResponse;
  }

  async stocksReceive(
    body: StockReceiveRequest,
  ): Promise<StockFindResponse> {
    const url = `${this.baseUrl}/api/v1/stocks/receive`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as StockFindResponse;
  }

  async stocksRelease(
    body: StockReleaseRequest,
  ): Promise<StockFindResponse> {
    const url = `${this.baseUrl}/api/v1/stocks/release`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as StockFindResponse;
  }

  async stocksReserve(
    body: StockReserveRequest,
  ): Promise<StockFindResponse> {
    const url = `${this.baseUrl}/api/v1/stocks/reserve`;
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
      body: JSON.stringify(body),
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as StockFindResponse;
  }

  async stocksRead(
    editionUid: string,
  ): Promise<StockFindResponse> {
    const url = `${this.baseUrl}/api/v1/stocks/${editionUid}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as StockFindResponse;
  }

  async stocksHistory(
    editionUid: string,
    warehouseUid: string | undefined,
    pageable: Pageable,
  ): Promise<StockHistoryFindAllResponse> {
    const params = new URLSearchParams();
    if (warehouseUid !== undefined) params.append('warehouse_uid', Array.isArray(warehouseUid) ? (warehouseUid as readonly unknown[]).join(',') : String(warehouseUid));
    if (pageable.page !== undefined) params.append('page', String(pageable.page));
    if (pageable.size !== undefined) params.append('size', String(pageable.size));
    if (pageable.sort !== undefined) params.append('sort', pageable.sort);
    const query = params.toString();
    const url = `${this.baseUrl}/api/v1/stocks/${editionUid}/history${query ? `?${query}` : ''}`;
    const response = await fetch(url, {
      method: 'GET',
      headers: {
        'Accept': 'application/json',
        ...(this.credentials.bearerAuth ? { 'Authorization': `Bearer ${this.credentials.bearerAuth}` } : {}),
      },
    });
    if (!response.ok) {
      const contentType = response.headers.get('content-type') ?? '';
      let body: unknown;
      try {
        if (contentType.includes('json')) {
          body = await response.json();
        } else if (contentType.startsWith('text/')) {
          body = await response.text();
        } else if (response.headers.get('content-length') !== '0') {
          body = await response.blob();
        }
      } catch {
        // Response body was unreadable / malformed — leave `body` undefined so the
        // caller still gets the status and can inspect headers if needed.
      }
      throw new ApiError(response.status, response.statusText, body);
    }
    return await response.json() as StockHistoryFindAllResponse;
  }
}
