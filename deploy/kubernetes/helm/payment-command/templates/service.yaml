apiVersion: v1
kind: Service
metadata:
  name: {{ include "payment-command.fullname" . }}
  labels:
    {{- include "payment-command.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "payment-command.selectorLabels" . | nindent 4 }}
