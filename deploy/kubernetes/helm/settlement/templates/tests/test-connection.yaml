apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "settlement.fullname" . }}-test-connection"
  labels:
    {{- include "settlement.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "settlement.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
