apiVersion: v1
kind: Service
metadata:
  name: {{ include "inventory-query.fullname" . }}
  labels:
    {{- include "inventory-query.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inventory-query.selectorLabels" . | nindent 4 }}
