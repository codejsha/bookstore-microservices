apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inventory-query.fullname" . }}-test-connection"
  labels:
    {{- include "inventory-query.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inventory-query.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
