apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "payment-query.fullname" . }}-test-connection"
  labels:
    {{- include "payment-query.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "payment-query.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
