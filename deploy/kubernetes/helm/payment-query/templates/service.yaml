apiVersion: v1
kind: Service
metadata:
  name: {{ include "payment-query.fullname" . }}
  labels:
    {{- include "payment-query.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "payment-query.selectorLabels" . | nindent 4 }}
