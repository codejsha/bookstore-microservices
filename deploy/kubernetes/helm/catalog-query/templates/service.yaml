apiVersion: v1
kind: Service
metadata:
  name: {{ include "catalog-query.fullname" . }}
  labels:
    {{- include "catalog-query.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "catalog-query.selectorLabels" . | nindent 4 }}
