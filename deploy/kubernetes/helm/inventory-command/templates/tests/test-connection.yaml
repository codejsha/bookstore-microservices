apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inventory-command.fullname" . }}-test-connection"
  labels:
    {{- include "inventory-command.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inventory-command.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
