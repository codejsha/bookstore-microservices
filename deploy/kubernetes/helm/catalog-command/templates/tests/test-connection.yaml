apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "catalog-command.fullname" . }}-test-connection"
  labels:
    {{- include "catalog-command.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "catalog-command.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
