apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "customer-query.fullname" . }}-test-connection"
  labels:
    {{- include "customer-query.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "customer-query.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
