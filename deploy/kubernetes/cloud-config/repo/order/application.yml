server:
  port: 8080

spring:
  threads:
    virtual:
      enabled: true
  flyway:
    enabled: false

temporal:
  worker:
    max-concurrent-activity-executions: 16
    max-concurrent-workflow-task-executions: 32
    activity-task-pollers: 2
    workflow-task-pollers: 2
