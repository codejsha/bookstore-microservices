spring:
  application:
    name: order
    version: 1.0.0
---
spring:
  config:
    activate:
      on-profile: local
    import: configserver:http://localhost:8888
  application:
    name: order
    version: 1.0.0
