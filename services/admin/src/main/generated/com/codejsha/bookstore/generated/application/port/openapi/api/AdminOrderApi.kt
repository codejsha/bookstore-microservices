// Generated by oapi-codegen-cli (1.0.0)

package com.codejsha.bookstore.generated.application.port.openapi.api

import com.codejsha.bookstore.generated.application.port.openapi.model.AdminOrderFindAllResponse
import com.codejsha.bookstore.generated.application.port.openapi.model.AdminOrderResponse
import org.springframework.data.domain.Pageable
import org.springframework.http.ResponseEntity
import org.springframework.security.access.prepost.PreAuthorize
import org.springframework.web.bind.annotation.GetMapping
import org.springframework.web.bind.annotation.PathVariable
import org.springframework.web.bind.annotation.PostMapping
import org.springframework.web.bind.annotation.RequestParam

/**
 * AdminOrder API
 */
interface AdminOrderApi {

    /**
     * List orders across all customers. Omit user_uid for the unscoped list.
     * Errors:
     * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
     * 401 (UnauthorizedError) — Access is unauthorized.
     * 403 (ForbiddenError) — Access is forbidden.
     *
     * GET /api/v1/admin/orders
     * @param userUid @RequestParam(value = "user_uid", required = false)
     * @param status @RequestParam(value = "status", required = false)
     * @param pageable 
     * @return ResponseEntity<AdminOrderFindAllResponse>
     */
    @PreAuthorize("isAuthenticated()")
    @GetMapping(
        value = ["/api/v1/admin/orders"],
        produces = ["application/json"]
    )
    fun adminOrdersListOrders(
        @RequestParam(value = "user_uid", required = false) userUid: String?,
        @RequestParam(value = "status", required = false) status: String?,
        pageable: Pageable?
    ): ResponseEntity<AdminOrderFindAllResponse>

    /**
     * Find an order by uid, with its lines and shipping destination
     * Errors:
     * 401 (UnauthorizedError) — Access is unauthorized.
     * 403 (ForbiddenError) — Access is forbidden.
     * 404 (NotFoundError) — The server cannot find the requested resource.
     *
     * GET /api/v1/admin/orders/{uid}
     * @param uid @PathVariable("uid")
     * @return ResponseEntity<AdminOrderResponse>
     */
    @PreAuthorize("isAuthenticated()")
    @GetMapping(
        value = ["/api/v1/admin/orders/{uid}"],
        produces = ["application/json"]
    )
    fun adminOrdersReadOrder(
        @PathVariable("uid") uid: String
    ): ResponseEntity<AdminOrderResponse>

    /**
     * Cancel an order. The order service runs the compensation, so the returned order reflects its state at that moment.
     * Errors:
     * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
     * 401 (UnauthorizedError) — Access is unauthorized.
     * 403 (ForbiddenError) — Access is forbidden.
     * 404 (NotFoundError) — The server cannot find the requested resource.
     *
     * POST /api/v1/admin/orders/{uid}/cancel
     * @param uid @PathVariable("uid")
     * @return ResponseEntity<AdminOrderResponse>
     */
    @PreAuthorize("isAuthenticated()")
    @PostMapping(
        value = ["/api/v1/admin/orders/{uid}/cancel"],
        produces = ["application/json"]
    )
    fun adminOrdersCancelOrder(
        @PathVariable("uid") uid: String
    ): ResponseEntity<AdminOrderResponse>
}
