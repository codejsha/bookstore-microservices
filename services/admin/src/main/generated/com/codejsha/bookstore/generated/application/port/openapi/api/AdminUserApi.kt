// Generated by oapi-codegen-cli (1.0.0)

package com.codejsha.bookstore.generated.application.port.openapi.api

import com.codejsha.bookstore.generated.application.port.openapi.model.AdminUserFindAllResponse
import com.codejsha.bookstore.generated.application.port.openapi.model.AdminUserResponse
import com.codejsha.bookstore.generated.application.port.openapi.model.AdminUserRolesRequest
import jakarta.validation.Valid
import org.springframework.data.domain.Pageable
import org.springframework.http.ResponseEntity
import org.springframework.security.access.prepost.PreAuthorize
import org.springframework.web.bind.annotation.GetMapping
import org.springframework.web.bind.annotation.PathVariable
import org.springframework.web.bind.annotation.PostMapping
import org.springframework.web.bind.annotation.PutMapping
import org.springframework.web.bind.annotation.RequestBody
import org.springframework.web.bind.annotation.RequestParam

/**
 * AdminUser API
 */
interface AdminUserApi {

    /**
     * List user accounts
     * Errors:
     * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
     * 401 (UnauthorizedError) — Access is unauthorized.
     * 403 (ForbiddenError) — Access is forbidden.
     *
     * GET /api/v1/admin/users
     * @param email @RequestParam(value = "email", required = false)
     * @param name @RequestParam(value = "name", required = false)
     * @param phone @RequestParam(value = "phone", required = false)
     * @param status @RequestParam(value = "status", required = false)
     * @param pageable 
     * @return ResponseEntity<AdminUserFindAllResponse>
     */
    @PreAuthorize("isAuthenticated()")
    @GetMapping(
        value = ["/api/v1/admin/users"],
        produces = ["application/json"]
    )
    fun adminUsersListUsers(
        @RequestParam(value = "email", required = false) email: String?,
        @RequestParam(value = "name", required = false) name: String?,
        @RequestParam(value = "phone", required = false) phone: String?,
        @RequestParam(value = "status", required = false) status: String?,
        pageable: Pageable?
    ): ResponseEntity<AdminUserFindAllResponse>

    /**
     * Find a user by uid
     * Errors:
     * 401 (UnauthorizedError) — Access is unauthorized.
     * 403 (ForbiddenError) — Access is forbidden.
     * 404 (NotFoundError) — The server cannot find the requested resource.
     *
     * GET /api/v1/admin/users/{uid}
     * @param uid @PathVariable("uid")
     * @return ResponseEntity<AdminUserResponse>
     */
    @PreAuthorize("isAuthenticated()")
    @GetMapping(
        value = ["/api/v1/admin/users/{uid}"],
        produces = ["application/json"]
    )
    fun adminUsersReadUser(
        @PathVariable("uid") uid: String
    ): ResponseEntity<AdminUserResponse>

    /**
     * Deactivate an account (deleted in Keycloak, soft-deleted locally). An administrator may not deactivate themselves.
     * Errors:
     * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
     * 401 (UnauthorizedError) — Access is unauthorized.
     * 403 (ForbiddenError) — Access is forbidden.
     * 404 (NotFoundError) — The server cannot find the requested resource.
     *
     * POST /api/v1/admin/users/{uid}/deactivate
     * @param uid @PathVariable("uid")
     * @return ResponseEntity<AdminUserResponse>
     */
    @PreAuthorize("isAuthenticated()")
    @PostMapping(
        value = ["/api/v1/admin/users/{uid}/deactivate"],
        produces = ["application/json"]
    )
    fun adminUsersDeactivateUser(
        @PathVariable("uid") uid: String
    ): ResponseEntity<AdminUserResponse>

    /**
     * Reactivate a suspended account
     * Errors:
     * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
     * 401 (UnauthorizedError) — Access is unauthorized.
     * 403 (ForbiddenError) — Access is forbidden.
     * 404 (NotFoundError) — The server cannot find the requested resource.
     *
     * POST /api/v1/admin/users/{uid}/reactivate
     * @param uid @PathVariable("uid")
     * @return ResponseEntity<AdminUserResponse>
     */
    @PreAuthorize("isAuthenticated()")
    @PostMapping(
        value = ["/api/v1/admin/users/{uid}/reactivate"],
        produces = ["application/json"]
    )
    fun adminUsersReactivateUser(
        @PathVariable("uid") uid: String
    ): ResponseEntity<AdminUserResponse>

    /**
     * Replace a user's roles. An administrator may not change their own roles.
     * Errors:
     * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
     * 401 (UnauthorizedError) — Access is unauthorized.
     * 403 (ForbiddenError) — Access is forbidden.
     * 404 (NotFoundError) — The server cannot find the requested resource.
     * 409 (ConflictError) — The request conflicts with the current state of the server.
     *
     * PUT /api/v1/admin/users/{uid}/roles
     * @param uid @PathVariable("uid")
     * @param requestBody @Valid @RequestBody
     * @return ResponseEntity<AdminUserResponse>
     */
    @PreAuthorize("isAuthenticated()")
    @PutMapping(
        value = ["/api/v1/admin/users/{uid}/roles"],
        consumes = ["application/json"],
        produces = ["application/json"]
    )
    fun adminUsersUpdateUserRoles(
        @PathVariable("uid") uid: String,
        @Valid @RequestBody requestBody: AdminUserRolesRequest
    ): ResponseEntity<AdminUserResponse>

    /**
     * Suspend an account (disabled in Keycloak). An administrator may not suspend themselves.
     * Errors:
     * 400 (BadRequestError) — The server could not understand the request due to invalid syntax.
     * 401 (UnauthorizedError) — Access is unauthorized.
     * 403 (ForbiddenError) — Access is forbidden.
     * 404 (NotFoundError) — The server cannot find the requested resource.
     * 409 (ConflictError) — The request conflicts with the current state of the server.
     *
     * POST /api/v1/admin/users/{uid}/suspend
     * @param uid @PathVariable("uid")
     * @return ResponseEntity<AdminUserResponse>
     */
    @PreAuthorize("isAuthenticated()")
    @PostMapping(
        value = ["/api/v1/admin/users/{uid}/suspend"],
        produces = ["application/json"]
    )
    fun adminUsersSuspendUser(
        @PathVariable("uid") uid: String
    ): ResponseEntity<AdminUserResponse>
}
